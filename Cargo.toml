[workspace]
resolver = "2"
members = [
    "links",
    "constellation",
    "jetstream",
    "ufos",
    "ufos/fuzz",
    "spacedust",
    "slingshot",
    "quasar",
    "pocket",
]

[workspace.dependencies]
clap = { version = "4.5.56", features = ["derive", "env"] }
